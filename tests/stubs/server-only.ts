/**
 * Test stub for `server-only`.
 *
 * The real package throws when evaluated outside a React Server Component. Unit
 * tests import server modules directly, so this no-op stands in for it.
 */
export {};
